import { StyleSheet, Text, View } from 'react-native';

export default function Die () {
    return(
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>This is a dice.</Text>
        </View>
    )
}